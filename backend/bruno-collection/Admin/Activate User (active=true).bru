meta {
  name: Activate User (active=true)
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/admin/users/:id/status
  body: json
  auth: inherit
}

params:path {
  id: 69dc28aeffcb47be8e37a2fd
}

body:json {
  { "accountStatus": "active" }
}

settings {
  encodeUrl: true
  timeout: 0
}
