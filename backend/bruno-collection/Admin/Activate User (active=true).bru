meta {
  name: Activate User (active=true)
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/admin/users/:id/status
  body: json
  auth: inherit
}

params:path {
  id: 69b712ffb844d4c248328435
}

body:json {
  { "accountStatus": "active" }
}

settings {
  encodeUrl: true
  timeout: 0
}
